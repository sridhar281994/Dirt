name: Build Android App

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      USER: root
      HOME_DIR: /root    

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Free Disk Space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo rm -rf "/usr/local/share/boost"
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"

      - name: Build with Buildozer
        uses: ArtemSBulgakov/buildozer-action@v1
        id: buildozer
        env:
          USER: root
          PIP_BREAK_SYSTEM_PACKAGES: "1"
        with:
          # buildozer-action installs buildozer via `pip --user`, which places the
          # binary in $HOME/.local/bin (not always on PATH for the container shell).
          # Pipe "yes" to avoid interactive Android SDK license prompts from sdkmanager.
          command: export PATH="$HOME/.local/bin:$PATH" && yes | buildozer android debug
          buildozer_version: master

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app
          path: bin/*.apk
